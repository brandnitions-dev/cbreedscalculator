# MOSSKYN LAB calculator — notes for future sessions

## Git (read this before “did you push?” checks)
- The Git repository root is THIS folder: `crownbreedscalculator/`
- Parent folder `random agent/` is NOT a git repo — do not run `git status` only there and conclude there is no repo.
- Remote: `origin` → https://github.com/brandnitions-dev/cbreedscalculator.git
- Default branch: `main`
- When the user asks to commit/push calculator changes, `cd` into `crownbreedscalculator` then `git add` / `commit` / `push`.

## Production & data — Netlify, Neon, VPS (keep in sync when setup changes)
**Last updated:** 2026-05-01

### Canonical live app (Docker on VPS)
- **Public URL:** https://mosskynlab.brandnitions.agency — spelling is **`brandnitions`** (with **n**), not “branditions”.
- **VPS:** `194.62.157.119` — this dev machine is typically **SSH-auth’d** as `root` (BatchMode / key auth).
- **App root on server:** `/opt/mosskynlab` (Docker Compose stack).
- **Compose:** services `web` + `db` (Postgres **not** exposed on host; DB only on Docker network `mosskynlab_internal`).
- **App port:** host `127.0.0.1:4050` → container `3000`; host **Nginx** reverse-proxies HTTPS for the domain; **Let’s Encrypt** certs managed on the VPS (same pattern as other `*.brandnitions.agency` sites).
- **Secrets:** live in `/opt/mosskynlab/.env` (`POSTGRES_PASSWORD`, `DATABASE_URL`, `NEXTAUTH_URL`, `NEXTAUTH_SECRET`) — **never commit**; tarball/deploy excludes `.env`.
- **Seeded admin (Prisma seed):** `admin@mosskynlab.com` — saved formulas are per **user id**; migrations should map to this email on the VPS DB when copying from Neon.

### Neon (managed Postgres — formula source & old Netlify DB)
- **Org id (neonctl, non-interactive):** `org-holy-shadow-57548995`
- **Project id:** `calm-tooth-00446342` (Neon console name e.g. **mosskynlab**)
- **Branch:** `production`
- **CLI connection string (pooled, Prisma):**  
  `npx neonctl connection-string production --project-id calm-tooth-00446342 --pooled --prisma --org-id org-holy-shadow-57548995`
- **Role:** Holds the **original** saved formulas (and is still the DB behind the **Netlify** test site below). **VPS production uses local Postgres in Docker**, not Neon.

### Netlify (separate site — not the VPS)
- **Linked project (CLI `netlify status`):** `crownbreeds-calc-2026`
- **Site ID:** `96a5d2e1-6833-45b0-9552-0e3f0a54bfe4`
- **Default URL:** https://crownbreeds-calc-2026.netlify.app
- **Team:** `altasbenda` (account email used for Netlify is **not** necessarily the same as lab login).
- **Production env:** `DATABASE_URL` points at **Neon** (not the VPS Postgres). `NEXTAUTH_URL` must match the Netlify host if you use that site.
- **Repo helpers:** `npm run netlify:wire-neon` → `scripts/wire-neon-to-netlify.cjs`; `npm run db:sync:netlify` → `scripts/sync-neon-from-netlify.cjs`.

### Saved formulas: Neon → VPS Postgres
- **Why:** `/formulas` is empty on the VPS if the Docker DB was never seeded with `Formula` rows; API filters by **logged-in user**.
- **Local one-shot (need VPS `DATABASE_URL`):** `TARGET_DATABASE_URL=...` `MIGRATE_FORMULAS_OWNER_EMAIL=admin@mosskynlab.com` `npm run db:migrate-formulas:to-vps` (`scripts/migrate-neon-to-vps.cjs` pulls Neon as source).
- **On VPS (Postgres not on host):** copy `scripts/migrate-formulas-neon-to-neon.ts` + a file containing Neon URL to e.g. `/tmp/`, `docker cp` into `web` container, `docker compose exec web` with `SOURCE_DATABASE_URL` and `TARGET_DATABASE_URL=$DATABASE_URL`. Repeatable shell: `scripts/vps-run-migrate-formulas.sh` (expects `/tmp/neon_source_url.txt` + script on server).
- **Core script:** `scripts/migrate-formulas-neon-to-neon.ts` (generic two-URL migrate).

### Cursor / deploy history (parent transcript IDs — for humans)
- **VPS Docker + nginx + `mosskynlab.brandnitions.agency`:** [177dc7f1-c5b9-4da2-9b4c-7e4c06b66f2e](177dc7f1-c5b9-4da2-9b4c-7e4c06b66f2e)
- **Netlify / Neon / formulas UI / Lips–Eyes / Purge preset (long thread):** [ff43f2e6-7ab9-4534-af3c-04c89e694ded](ff43f2e6-7ab9-4534-af3c-04c89e694ded)

## Which HTML files matter
- `tallow_blend_pro_v5.html` — main tallow blend tool (includes 50 ml pack-out / jar UI).
- `index.html` — should stay in sync with `tallow_blend_pro_v5.html` when that is the deployed entry (copy or identical changes).
- `crownbreeds_formula_builder_v3.html` — separate formula builder page; pack-out / jar redesign lives in `tallow_blend_pro_v5.html`, not here unless explicitly ported.

## 50 ml pack-out UI (2025 redesign)
- Markup: wrapper `.jar-packout`, `#chips.jar-preview`, `#chips-note.jar-packout__footer`.
- JS: `renderJarPreview()` builds `.jar-packout__grid`, `.jar-packout__stats` with `.jar-stat` (primary + remainder), `.jar-packout__visual` + `buildSingleJarCard()`.
- Old pattern removed: `jar-count-badge`, `jar-leftover-strip` — do not reintroduce without restoring matching CSS.

## Exfoliator Upgrade (2025-03-31)
### Formula Builder changes
- **Beeswax optional** in both balm and exfoliator modes (checkbox `#fb-beeswax-chk`). When off, 8% goes to tallow.
- **Ingredient A & B optional** in exfoliator/scrub mode. When pools are empty, their % redistributes to tallow. Labels show "(optional — ratio goes to tallow if empty)".
- **Ginger** added to Ingredient C list in `formula_builder_c_list.js` with powder/oil form selector.
- **Powder/oil form selector** on C ingredients: coffee grounds, clay, ginger. Form buttons rendered in `fbRenderPool("c")`. Oil-form items show "(oil phase)" note. Form-specific benefits used in `fbUpdateBenefits()`.
- CSS for `.fb-form-row`, `.fb-form-btn`, `.fb-form-note`, `.fb-beeswax-toggle`, `.fb-opt-label` added to `tallow_blend_pro_v5.html`.
- `fbCalcFormula()` rewritten: balm mode respects `fbBeeswax` toggle; scrub mode handles optional A/B/beeswax with tallow absorption.

### Lab Worksheet changes
- **Product type selector** added: Skin Balm (default) / Exfoliator (`#lab-product-type`).
- **Beeswax toggle** (`#lab-beeswax-toggle`) — optional in both modes, ratio goes to tallow.
- **Ingredient A/B toggles** (`#lab-ing-a-toggle`, `#lab-ing-b-toggle`) — shown only in exfoliator mode, ratio goes to tallow when unchecked.
- **Ingredient C row** added to worksheet (hidden by default, shown in exfoliator mode) with group header, accent bar, and ml display.
- **C strength slider** (`#lab-c-strength`) — 5–15% range, shown in exfoliator mode.
- **Composition bar** — added `#b-c` segment and `#leg-c` legend entry.
- `calcBalanced()` rewritten: handles scrub mode (C phase, optional A/B/beeswax with tallow absorption + scale-down).
- `calcFromTallowBal()` updated for dynamic tallow fraction.
- `buildCompositionSeries()` dynamically includes/excludes A, B, C, beeswax based on values.
- `renderSummary()` conditionally shows A, B, C rows.
- `render()` handles optional beeswax display, C display, updated pills and tallow label.
- `renderProcSteps()` handles exfoliator steps (fold in C last) and optional beeswax in melt step.
- Helper functions added: `labIsScrub()`, `labBeeswaxOn()`, `labIngAOn()`, `labIngBOn()`, `labCPct()`, `onLabProductChange()`, `onLabCStrengthChange()`.

### Chemistry: Powder vs Oil-form C (oil absorption tax)
- **Dry powders absorb oil from tallow.** Each C ingredient has `oilAbsorb` (g oil per g powder):
  - Sugar: 0.08, Baking soda: 0.12, Walnut/shell: 0.15, Coffee grounds: 0.30, Ginger powder: 0.35, Oat flour: 0.40, Clay: 0.50
- **Oil-form C** (infused oil, paste) displaces carrier oil 1:1 — `oilAbsorb: 0`.
- `fbCalcFormula()` computes `oilTax = sum(cRow.pct × oilAbsorb)` and adds it to tallow.
- Lab Worksheet uses fixed `LAB_AVG_OIL_ABSORB = 0.25` (midrange powder) since it doesn't track individual C items.
- `calcFromTallowBal()` includes `cPctVal * 0.25` in `talFrac` so tallow→jojoba back-solve is stable.
- Form selector UI shows: powder → "(dry — absorbs X% its weight in oil from tallow)", oil → "(oil phase — displaces carrier 1:1)".
- Each C ingredient has `phase: "dry"` or `phase: "oil"` (multi-form items carry phase in `formMeta`).

### Chemistry: Potency-aware maxPct caps
- Each C ingredient has `maxPct` (max safe % of total batch for that single ingredient).
- Multi-form items carry `maxPct` in `formMeta` — powder forms get stricter caps than oil forms.
- Caps: Sugar 15%, Oat flour 12%, Coffee powder 12% / oil 15%, Clay powder 8% / oil 12%, Baking soda 8%, Walnut/shell 6%, **Ginger powder 5%** / oil 12%.
- `fbCalcEffectiveMaxC()` computes weighted-average max C% across the pool — e.g., ginger powder (5%) + sugar (15%) at equal weights → ~10% effective max.
- `fbCalcFormula()` clamps `cPct` to the effective max. `fbUpdate()` dynamically sets the slider's `max` attribute and shows "capped by potent ingredient" note.
- `fbUpdateSynergies()` warns which ingredient is limiting and suggests switching to oil-form for higher safe range.
- Lab Worksheet `labCPct()` also clamps via `fbCalcEffectiveMaxC()`. `onLabCStrengthChange()` clamps the lab slider.
- Helper: `fbGetCMaxPct(row)` resolves maxPct for a row (formMeta first, then top-level, fallback 0.15).

### Chemistry: Structural & therapeutic warnings
- **No beeswax** — scrub has no structural hold at room temp; loose oil-paste. Warned in synergies + production steps.
- **No A + B** — 100% tallow base with no therapeutic carriers. Warned in synergies.
- **High total oil absorption (>3%)** — scrub may feel dry/stiff. Warned with suggestion to use oil-form or add carrier A.
- **Mixed dry + oil C** — oil-form dissolves into base, dry sits suspended. Fold dry in last.

### Files touched
- `formula_builder_c_list.js` — added ginger, forms/formMeta on coffee/clay/ginger, oilAbsorb + phase on all items
- `formula_builder_tab.js` — fbBeeswax state, fbToggleBeeswax(), fbUpdateOptionalLabels(), fbCalcFormula() with oilTax, fbGetCOilAbsorb(), fbGetCPhase(), fbRenderPool() form selector with absorption note, fbUpdateBenefits() form-aware, fbUpdateSynergies() chemistry warnings
- `tallow_blend_pro_v5.html` — CSS for form selector/beeswax toggle, HTML for beeswax toggle + optional A/B labels + product type + C controls + C worksheet row + C bar/legend, JS for all calc/render functions with oil absorption tax
- `index.html` — synced from `tallow_blend_pro_v5.html`

## Skin Chemistry Panel Fix (2025-04-10)
### Issue
- `#panel-skin-chemistry` was using CSS variables (`--color-text-primary`, `--color-background-info`, etc.) that were only defined inside `#panel-builder`, causing broken styling (missing colors, backgrounds, borders).

### Fix
- Added CSS variable definitions to `#panel-skin-chemistry` matching the design system:
  - `--color-text-primary/secondary/tertiary` mapped to `--text`/`--text-muted`
  - `--color-text-info/success/warning/danger` with light/dark mode variants
  - `--color-background-primary/secondary/info/success/warning/danger` with proper alpha values
  - `--color-border-primary/secondary/tertiary` mapped to `--border-strong`/`--border`
  - `--border-radius-md/lg` mapped to `--radius-md`/`--radius-lg`
- Added dark mode media query overrides for semantic colors
- Added explicit `color` to `.layer-name` and `.layer-detail` classes

### Files touched
- `tallow_blend_pro_v5.html` — CSS variables + dark mode overrides for `#panel-skin-chemistry`, color fixes for `.layer-name`/`.layer-detail`
- `index.html` — synced from `tallow_blend_pro_v5.html`
- `skin_chemistry_blueprint.html` — synced `.layer-name`/`.layer-detail` color fixes

## Full Restructuring (2025-04-11)
### Architecture Change
- **Two-tier navigation** implemented:
  - **Top row**: Product categories (Tallow Balms, Treatment Oils, Soaps)
  - **Bottom row**: Tool tabs (Lab worksheet, Formula builder, Skin chemistry)
- Each product category has its own set of panels (lab, builder, chemistry)
- URL hash format: `#category-tool` (e.g., `#oils-lab`, `#balms-builder`)

### Product Categories
1. **Tallow Balms** (`balms`)
   - Skin Balm (58% tallow, 8% beeswax, 11% jojoba, 13% A, 6% B, 0.4% VitE, 1-2% EO)
   - Exfoliator/Scrub (scaled ratios + 5-15% Ingredient C)
2. **Treatment Oils** (`oils`)
   - BHA Penetrating Oil (home/spa): Jojoba 60%, Rosehip 20%, Willow Bark 10%, Frankincense 4%, Wintergreen 3%, Tea Tree 3%
   - Pore Purge Blend (clinical): Jojoba 50%, Squalane 20%, Castor 15%, Grapeseed 10%, Rosehip 3.5%, Alpha-Bisabolol 0.5%, Wintergreen 0.5%, Tea Tree 0.5%, Frankincense 0.5%
3. **Soaps** (`soaps`) — Coming soon placeholder

### New Files
- `product_config.js` — Centralized product definitions, formulas, ingredients

### New Panels Added (Full Structure Matching Tallow Balms)
- `#panel-oils-lab` — Full lab worksheet:
  - Section 1: Setup & controls (formula type, batch size, bottle size selector: 5/10/15/20/30/50ml)
  - Section 2: Formulation worksheet (ingredient rows with color accents, ml values, percentages)
  - Section 3: Batch summary (composition bar, legend, summary table, doughnut chart)
  - Section 4: Production (cold blend process steps, packout with bottle chips)
- `#panel-oils-builder` — Full formula builder:
  - Batch size slider, formula base selector (BHA/Pore Purge/Custom)
  - Carrier oils pool, Actives pool, Essential oils pool
  - Beaker visualization with layered SVG
  - Ratio breakdown, treatment profile, notes & warnings
- `#panel-oils-chemistry` — Treatment oils chemistry with tabs: Pore anatomy, BHA/Salicylates, Carrier penetration, Clinical protocol, Safety warnings

### Key Chemistry Notes (Treatment Oils)
- **Wintergreen EO**: Max 3% home, 0.5% clinical — methyl salicylate is toxic at higher doses (1 tsp = ~7g aspirin)
- **Castor + Squalane + Wintergreen**: Creates chemical reaction mimicking 15min steam + desincrustation fluid
- **Pore Purge Blend**: Clinicians feel blackheads "rolling like grains of sand" during massage
- **BHA increases sun sensitivity**: SPF 30+ required after treatment

### Files touched
- `tallow_blend_pro_v5.html` — Two-tier nav, Treatment Oils panels (lab/builder/chemistry), CSS variables, JS functions
- `product_config.js` — New file with product definitions
- `index.html` — Needs sync from `tallow_blend_pro_v5.html`

## Next.js Revamp (2025-04-11)
### Architecture
- **Tailwind v3** (3.4.17) with PostCSS — v4 caused OOM, reverted
- **tailwind.config.ts** — custom color tokens: surface-*, border-*, text-*, accent-*
- **postcss.config.mjs** — tailwindcss + autoprefixer
- **next.config.js** — `experimental.workerThreads: false, cpus: 1` to prevent OOM on build
- **NODE_OPTIONS=--max-old-space-size=4096** required for production build

### Shared UI Components (src/components/ui/)
- `GlassCard` — glass morphism card with optional `interactive` hover lift
- `Badge` — pill with variants: indigo, gold, emerald, rose, sky
- `Tabs` + `TabPanel` — tabbed interface

### Shared Formula Components (src/components/formula/)
- `IngredientChipGrid` — NEW: click-to-toggle chips + expandable detail cards (replaces dropdown IngredientPool)
- `IngredientPool` — LEGACY dropdown/slider, still exported but replaced by ChipGrid on all pages
- `BeakerViz` — SVG layered beaker visualization
- `BenefitBars` — horizontal benefit score bars
- `RatioBars` — ingredient ratio bars with ml/pct
- `SynergyPanel` — synergy notes & warnings with icons
- `PackoutGrid` — batch packout with unit count + remainder
- `CompositionBar` — segmented formula composition bar

### Shared Layout (src/components/layout/)
- `ProductCalculator` — wraps Lab/Builder/Chemistry tabs with header

### 4-Product Taxonomy (2025-04-11)
1. **Tallow Balm** (`/balm`) — Face/Body, tallow + beeswax + jojoba + carriers + actives + EOs
2. **Face Cleaner** (`/cleaner`) — Tallow-based scrub with physical exfoliant C-phase (sugar/coffee/clay)
3. **Exfoliator** (`/exfoliator`) — BHA penetrating oil / Pore Purge, NO tallow, carrier oils + actives + EOs
4. **Tallow Soap** (`/soap`) — Cold process soap with lye calculations

- Old `/oils` page REMOVED (was Treatment Oils — replaced by `/exfoliator`)
- Old `/exfoliator` page content moved to `/cleaner` (was tallow scrub, not real exfoliation)

### Ingredient Data
- `src/lib/ingredients/carriers-a.ts` — 21 carrier oils (shared: Balm, Cleaner)
- `src/lib/ingredients/actives-b.ts` — 15 active botanicals (shared: Balm, Cleaner)
- `src/lib/ingredients/essential-oils.ts` — 37 EOs (shared: Balm, Cleaner, Soap)
- `src/lib/ingredients/exfoliants-c.ts` — 6 physical exfoliants (Cleaner only)
- `src/lib/ingredients/treatment-oils.ts` — OIL_CARRIERS (8), OIL_ACTIVES (4), OIL_EOS (7), OIL_PRESETS (2) (Exfoliator only)
- `src/lib/ingredients/soap-oils.ts` — Soap oils with SAP values
- `src/lib/ingredients/soap-additives.ts` — Soap additives

### Sidebar Nav
- CALCULATORS: Dashboard, Tallow Balm, Face Cleaner, Exfoliator, Tallow Soap
- MANAGE: Saved Formulas, Admin

### Build Status
- All pages compile: /, /balm, /cleaner, /exfoliator, /soap, /builder, /formulas, /admin, /login
- Dev server: `npx next dev -p 3001`
- Production build: `$env:NODE_OPTIONS="--max-old-space-size=4096"; npx next build`

### ProductCalculator Wrap (2025-04-12)
All 4 products now use `ProductCalculator` wrapper with 3 consistent sub-tabs:
- **Lab Worksheet** — setup checklist, temperature protocol, batch record, safety warnings
- **Formula Builder** — the existing calculator (extracted to `formula-builder.tsx`)
- **Skin Chemistry** — science reference with sub-tabs for each topic area

Files created per product:
- `src/app/balm/` → `page.tsx`, `lab-worksheet.tsx`, `formula-builder.tsx`, `chemistry.tsx`
- `src/app/cleaner/` → `page.tsx`, `lab-worksheet.tsx`, `formula-builder.tsx`, `chemistry.tsx`
- `src/app/exfoliator/` → `page.tsx`, `lab-worksheet.tsx`, `formula-builder.tsx`, `chemistry.tsx`
- `src/app/soap/` → `page.tsx`, `lab-worksheet.tsx`, `formula-builder.tsx`, `chemistry.tsx`

Soap fully migrated from inline styles to Tailwind CSS.
Dashboard updated for 4-product taxonomy (Face Cleaner + Exfoliator replacing old Exfoliator/Scrub + Treatment Oils).
Build passes cleanly (12 static pages, 0 errors).

### Remaining
- Builder page (`/builder`) may be deprecated — each product has own formula builder now
- Old `/oils` directory can be cleaned up if still present
- Git commit + push pending

## Eye Revival Serum Formula Audit (2026-04-29)
### Problem
Original eye serum formula (eyes mode in balm builder) had critical flaws:
- No emulsifier — 60% water + 28% oil will separate immediately
- No preservative — water-based formula at severe microbial risk near eyes
- Oil phase 28% too heavy for eye area (standard: 5–15%)
- Vitamin E at 2% overdosed (should be 0.5% antioxidant, not preservative)
- Licorice root at 3.2% above safe range (max 2%)
- No pH specification (actives are pH-sensitive)
- Wrong product classification ("Tallow Balm" — it contains no tallow)

### Corrected Formula (v2)
- Water phase 60.5%: Aloe 35%, Green tea hydrosol 20%, Glycerin 5.5%
- Oil phase ~14%: Squalane 8%, Jojoba 4%, Carrier A 9.4% (prickly pear + rosehip shared)
- Emulsifier 6%: Olivem 1000 (Cetearyl Olivate + Sorbitan Olivate) — EcoCert
- Preservative 1.5%: Geogard Ultra (Gluconolactone + Sodium Benzoate)
- Chelator 0.1%: Phytic acid (prevents rosehip oxidation)
- Vitamin E 0.5%: Oil-phase antioxidant, add before emulsification
- Actives 8%: Caffeine ~2.9%, Gotu kola ~2%, Licorice root ≤2%, Bakuchiol ~1%
- pH target: 5.0–5.5
- Shelf life: 6 months (3M PAO)

### Default Pool Changes (formula-builder.tsx)
- Carrier A: pricklypear weight 6→2, rosehip weight 4→2
- Active B: caffeine_extract weight 2→3, tremella→gotukola (weight 2), licorice_root weight 2 (unchanged), bakuchiol weight 1 (unchanged)

### Files Changed
- `src/lib/formula-engine.ts` — new fixed ingredients (emulsifier, preservative, chelator), corrected percentages, new color tokens
- `src/app/balm/formula-builder.tsx` — updated default pools and info banner
- `docs/compliance/inci-eye-serum.md` — NEW: full INCI label draft with manufacturing process

### Remaining (manual)
- Confirm licorice root extract solubility with supplier (water vs oil soluble)
- PET (Preservative Efficacy Test) required before selling
- Accelerated stability testing at 40°C/75% RH
- PIF must be updated: "Eye Serum / Leave-on emulsion" NOT "Tallow Balm"
- Bakuchiol at ~1% = no retinol-alternative claims (needs 2% for efficacy claims)

## Type B: Anhydrous Eye Oil Serum (2026-04-29, revised)
### Rationale
Pure anhydrous oil serum — zero tallow, zero beeswax, zero water.
Under-eye skin is 0.5mm thin. Tallow is too heavy/occlusive (causes milia).
Beeswax creates tacky film that blocks actives and interferes with makeup.
Squalane replaces tallow as the biomimetic oil for thin sensitive skin.

### Formula (mode: eyes_balm, UI label: "Eye Oil")
- Squalane 50% — biomimetic backbone, lightest carrier, oxidation-stable
- Jojoba 20% — sebum-mimetic, fast-absorbing
- Rosehip 10% — natural retinoids (vitamin A), skin renewal
- Prickly pear blend 8% — vitamin K for dark circles (Teamra source)
- Calendula oil 8% — anti-inflammatory, soothing (Cleopatra.al source)
- Vitamin E 2% — antioxidant, prevents rosehip oxidation
- No tallow, no beeswax, no essential oils, no water
- No emulsifier, no preservative, no pH control needed
- Shelf life: 12–18 months

### Supplier Sources
| Ingredient | Supplier | Status |
|---|---|---|
| Squalane | UNA Pharma | ✓ confirmed |
| Jojoba | Stock | ✓ confirmed |
| Rosehip | Stock | ✓ confirmed |
| Prickly pear blend | Teamra | adjust Vit E accordingly |
| Calendula oil | Cleopatra.al | ✓ confirmed |
| Vitamin E | Stock | ✓ confirmed |

### Key Differences vs Eye Serum
| | Eye Serum | Eye Oil |
|---|---|---|
| Base | Water-oil emulsion | Pure anhydrous oil |
| Tallow / Beeswax | None | None |
| Emulsifier | Olivem 1000 (6%) | Not needed |
| Preservative | Geogard Ultra (1.5%) | Not needed |
| pH control | Required (5.0–5.5) | Not applicable |
| Shelf life | 6 months | 12–18 months |
| Manufacturing | Complex (heat phases, emulsify, pH adjust) | Simple (blend oils at room temp) |
| Actives | Water + oil soluble (caffeine, etc.) | Oil-inherent (rosehip retinoids, prickly pear vit K) |
| Texture | Light water-based serum | Ultra-light liquid oil |
| Best for | Daytime, under makeup | Nighttime, intensive repair |

### Files Changed
- `src/lib/formula-engine.ts` — eyes_balm: pure oil serum, added rosehip/pricklypear/calendula color tokens
- `src/app/balm/formula-builder.tsx` — "Eye Oil" button, empty default pools, oil serum banner
- `src/app/formulas/page.tsx` — handles eyes_balm in saved formula display
- `src/types/index.ts` — 'eyes_balm' in FormulaResult product type

